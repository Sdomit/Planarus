# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg-info/
.eggs/
.pytest_cache/
.venv/
venv/
env/
.Python

# SQLite databases (never commit runtime data)
*.db
*.db-shm
*.db-wal

# Node / pnpm
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.tsbuildinfo

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Secrets — belt-and-suspenders (also in FILES_FORBIDDEN.md)
.env
.env.*
*.pem
*.key
secrets/

# Local QA artifacts (Playwright MCP screenshots/logs — not source)
.playwright-mcp/

# Local agent/tooling config — machine paths + capability ids, never published
.claude/
.mcp.json

# Launch/marketing working material — drafts, distribution notes, internal
# checklists. Kept locally, deliberately not part of the public repo.
docs/marketing/

# ---------------------------------------------------------------------------
# Working material kept on disk but out of the published repo.
#
# These are the agent operating pack and the internal build record. They are
# how this repo is *developed*, not part of what it *ships*, and several of
# them (NEXT_STEP, RISKS, the audit) are stale the moment they are pushed.
# A reference copy lives outside the repo; see docs/README.md.
#
# Note: untracking removes them from future commits only. Every commit made
# before this one still carries them, so they remain readable in the history
# of any public clone until that history is rewritten.
# ---------------------------------------------------------------------------
CLAUDE.md
AGENTS.md
deep-research-report.md
context/
docs/release-readiness-audit.md
docs/plan/
# `docs/dev/*`, not `docs/dev/` — git does not descend into an ignored
# directory, so the trailing-slash form would make the negation below dead.
docs/dev/*
!docs/dev/setup.md
